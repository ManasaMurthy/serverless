#!groovy

node{
    cleanWs()
 
    ws("/var/lib/jenkins/workspace/Configure-Project") {
        dir("${Service}") {
               sh "serverless deploy --aws-profile ${params.profile}"
        }
    }
   
}
